--[[
    Chocolate Sniper - Full Edition con Riddle Solver
    Cooldown 5s, priorización de 7 caracteres, auto submit tras retype, colapsable, riddles offline
]]

local cloneref = cloneref or function(object) return object end
local Players           = cloneref(game:GetService("Players"))
local ReplicatedStorage = cloneref(game:GetService("ReplicatedStorage"))
local RunService        = cloneref(game:GetService("RunService"))
local UserInputService  = cloneref(game:GetService("UserInputService"))
local TweenService      = cloneref(game:GetService("TweenService"))
local HttpService       = cloneref(game:GetService("HttpService"))
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

if getgenv and getgenv().StopAura then pcall(getgenv().StopAura) end

-- CONFIGURATION SYSTEM --
local CONFIG_FILE = "chocolate_sniper_config.json"
local savedConfig = {
    codeSniper = true,
    autoSubmit = true,
    submitAfter = 3,
    retypeInvalid = false,
    prioritize7 = false,
    autoSubmitAfterRetype = false,
    riddleSolver = false,
    collapsed = false,
}
pcall(function()
    if type(isfile) == "function" and type(readfile) == "function"
    and isfile(CONFIG_FILE) then
        local decoded = HttpService:JSONDecode(readfile(CONFIG_FILE))
        if type(decoded) == "table" then
            if type(decoded.codeSniper) == "boolean" then savedConfig.codeSniper = decoded.codeSniper end
            if type(decoded.autoSubmit) == "boolean" then savedConfig.autoSubmit = decoded.autoSubmit end
            if type(decoded.submitAfter) == "number" then savedConfig.submitAfter = math.max(1, math.floor(decoded.submitAfter)) end
            if type(decoded.retypeInvalid) == "boolean" then savedConfig.retypeInvalid = decoded.retypeInvalid end
            if type(decoded.prioritize7) == "boolean" then savedConfig.prioritize7 = decoded.prioritize7 end
            if type(decoded.autoSubmitAfterRetype) == "boolean" then savedConfig.autoSubmitAfterRetype = decoded.autoSubmitAfterRetype end
            if type(decoded.riddleSolver) == "boolean" then savedConfig.riddleSolver = decoded.riddleSolver end
            if type(decoded.collapsed) == "boolean" then savedConfig.collapsed = decoded.collapsed end
        end
    end
end)

local function saveConfig()
    if type(writefile) ~= "function" then return end
    pcall(function()
        writefile(CONFIG_FILE, HttpService:JSONEncode({
            codeSniper = savedConfig.codeSniper,
            autoSubmit = savedConfig.autoSubmit,
            submitAfter = savedConfig.submitAfter,
            retypeInvalid = savedConfig.retypeInvalid,
            prioritize7 = savedConfig.prioritize7,
            autoSubmitAfterRetype = savedConfig.autoSubmitAfterRetype,
            riddleSolver = savedConfig.riddleSolver,
            collapsed = savedConfig.collapsed,
        }))
    end)
end

-- STATE VARIABLES --
local _enabled              = savedConfig.codeSniper
local _seen                 = {}
local _focused              = nil
local _lastBox              = nil
local _autoAccept           = savedConfig.autoSubmit
local _submitAfter          = savedConfig.submitAfter
local _capturedParts        = {}
local _lastWatchedBox       = nil
local _boxTextConn          = nil
local _boxAncestryConn      = nil
local _boxVisibilityConns   = {}
local _retypeInvalid        = savedConfig.retypeInvalid
local _prioritize7          = savedConfig.prioritize7
local _autoSubmitAfterRetype = savedConfig.autoSubmitAfterRetype
local _riddleSolver         = savedConfig.riddleSolver
local _pendingRetypeSubmit  = false
local _collapsed            = savedConfig.collapsed
local _lastNonBlankBoxText  = ""
local _pendingRejectedText  = nil
local _pendingRejectedBox   = nil
local _pendingRejectedUntil = 0
local _pendingRejectedToken = 0
local _retypeCarryText      = nil
local _retypeCarryUntil     = 0
local _retypeCarryVersion   = 0
local ACE_CASE_MODE         = "EXACT"
local ACE_WORD_COUNT        = 1

-- RIDDLE SOLVER STATE --
local _solving      = 0
local _solvedCount  = 0
local _lastTypedSeq = 0
local _riddleSeq    = 0

-- COOLDOWN STATE --
local _cooldownActive = false
local _cooldownTask = nil

local getupvalues = (debug and debug.getupvalues) or getupvalues
local getconns    = getconnections or (debug and debug.getconnections)
local setupv      = (debug and debug.setupvalue) or setupvalue

local setStatus, flashCode, appendToBox
local rememberPendingSubmission, clearPendingSubmission, handleRedemptionFeedback
local clearAceCapture
local _lastStatusMsg = nil

-- Referencias a toggles para actualizar visualmente
local toggleRefs = {}

-- UTILITY & REDEEM LOGIC --
local function isOurGui(instance)
    local p = instance
    for _ = 1, 10 do
        if not p then break end
        if p.Name == "CardACECodeSniper" or p.Name == "ACECodeSniperUI" or p.Name == "SourcesHubRedeemerGui" then return true end
        p = p.Parent
    end
    return false
end

local function isVisibleChain(inst)
    local current = inst
    while current do
        if current:IsA("GuiObject") and not current.Visible then return false end
        if current:IsA("ScreenGui") then return current.Enabled end
        current = current.Parent
    end
    return true
end

local function findAllTextBoxes(pg)
    local boxes = {}
    for _, gui in ipairs(pg:GetChildren()) do
        if gui:IsA("ScreenGui") and gui.Enabled and not isOurGui(gui) then
            for _, d in ipairs(gui:GetDescendants()) do
                if d:IsA("TextBox") and not isOurGui(d) then
                    boxes[#boxes+1] = d
                end
            end
        end
    end
    return boxes
end

local function findCodeButtons(pg)
    local btns = {}
    for _, gui in ipairs(pg:GetChildren()) do
        if gui:IsA("ScreenGui") and gui.Enabled and not isOurGui(gui) then
            for _, d in ipairs(gui:GetDescendants()) do
                if (d:IsA("TextButton") or d:IsA("ImageButton")) and not isOurGui(d) then
                    local n  = d.Name:lower()
                    local pn = (d.Parent and d.Parent.Name or ""):lower()
                    if (n:find("code") or n:find("redeem") or pn:find("code") or pn:find("redeem"))
                        and isVisibleChain(d) then
                        btns[#btns+1] = d
                    end
                end
            end
        end
    end
    return btns
end

local function clickButton(btn)
    if not btn then return false end
    local methods = {}
    
    methods[#methods+1] = function() btn.MouseButton1Click:Fire() end
    methods[#methods+1] = function() btn.Activated:Fire() end
    
    if typeof(firesignal) == "function" then
        methods[#methods+1] = function() firesignal(btn.MouseButton1Click) end
        methods[#methods+1] = function() firesignal(btn.Activated) end
    end
    
    if typeof(getconns) == "function" then
        methods[#methods+1] = function()
            local ok, cs = pcall(getconns, btn.MouseButton1Click)
            if ok and type(cs) == "table" then
                for _, c in ipairs(cs) do pcall(function() c:Fire() end) end
            end
            local ok2, cs2 = pcall(getconns, btn.Activated)
            if ok2 and type(cs2) == "table" then
                for _, c in ipairs(cs2) do pcall(function() c:Fire() end) end
            end
        end
    end
    
    if typeof(fireclick) == "function" then
        methods[#methods+1] = function() fireclick(btn) end
    end
    
    local anyOk = false
    for _, fn in ipairs(methods) do
        local ok = pcall(fn)
        anyOk = anyOk or ok
    end
    return anyOk
end

local function fireBoxFocusLost(box)
    if not box then return false end
    local anyFired = false
    
    if typeof(firesignal) == "function" then
        local ok = pcall(firesignal, box.FocusLost, true)
        anyFired = anyFired or ok
    end
    
    if typeof(getconns) == "function" then
        local ok, cs = pcall(getconns, box.FocusLost)
        if ok and type(cs) == "table" then
            for _, c in ipairs(cs) do
                local fn
                pcall(function() fn = c.Function end)
                if fn and typeof(getupvalues) == "function" and typeof(setupv) == "function" then
                    local uOk, ups = pcall(getupvalues, fn)
                    if uOk and type(ups) == "table" then
                        for i, v in pairs(ups) do
                            if type(v) == "boolean" and v == true then
                                pcall(setupv, fn, i, false)
                            end
                        end
                    end
                end
                local fOk = pcall(function()
                    if c.Enabled ~= false then c:Fire(true) end
                end)
                anyFired = anyFired or fOk
            end
        end
    end
    
    return anyFired
end

local function typeAndSubmitCode(code)
    local pg = playerGui or player:FindFirstChildOfClass("PlayerGui")
    if not pg then return false, "no PlayerGui" end

    local codesGui = pg:FindFirstChild("Codes")
    if codesGui then
        if codesGui:IsA("ScreenGui") then codesGui.Enabled = true end
        local codesFrame = codesGui:FindFirstChild("Codes") or codesGui
        if codesFrame then
            if codesFrame:IsA("GuiObject") then codesFrame.Visible = true end
            local cur = codesFrame
            while cur and cur ~= codesGui do
                if cur:IsA("GuiObject") then cur.Visible = true end
                cur = cur.Parent
            end

            local box = nil
            for _, d in ipairs(codesFrame:GetDescendants()) do
                if d:IsA("TextBox") and not isOurGui(d) then
                    box = d
                    break
                end
            end

            local submitBtn = nil
            for _, d in ipairs(codesFrame:GetDescendants()) do
                if (d:IsA("TextButton") or d:IsA("ImageButton")) and not isOurGui(d) then
                    local n = d.Name:lower()
                    local txt = ""
                    pcall(function() txt = d.Text:lower() end)
                    if n:find("submit") or txt:find("submit") or n:find("redeem") or txt:find("redeem") or n:find("claim") or txt:find("confirm") or n:find("enter") then
                        submitBtn = d
                        break
                    end
                end
            end
            if not submitBtn then
                for _, d in ipairs(codesFrame:GetDescendants()) do
                    if (d:IsA("TextButton") or d:IsA("ImageButton")) and not isOurGui(d) then
                        local n = d.Name:lower()
                        if not n:find("close") and not n:find("x") and not n:find("toggle") then
                            submitBtn = d
                            break
                        end
                    end
                end
            end

            if box then
                pcall(function() box.Text = code end)
                task.wait(0.05)
                if submitBtn then clickButton(submitBtn) end
                fireBoxFocusLost(box)
                return true, "submitted via PlayerGui.Codes"
            end
        end
    end

    local btns = findCodeButtons(pg)
    for _, btn in ipairs(btns) do
        clickButton(btn)
        task.wait(0.05)
    end

    task.wait(0.2)

    local box = nil
    local deadline = tick() + 2
    while tick() < deadline do
        local allBoxes = findAllTextBoxes(pg)
        for _, d in ipairs(allBoxes) do
            if isVisibleChain(d) then
                local n  = d.Name:lower()
                local pn = (d.Parent and d.Parent.Name or ""):lower()
                if n:find("code") or pn:find("code") or n:find("redeem") or pn:find("redeem") or n:find("input") or pn:find("textbox") or n:find("enter") then
                    box = d
                    break
                end
            end
        end
        if not box then
            for _, d in ipairs(allBoxes) do
                if isVisibleChain(d) then box = d; break end
            end
        end
        if box then break end
        task.wait(0.1)
    end

    if not box then return false, "no codebox visible" end

    pcall(function() box.Text = code end)
    task.wait(0.05)

    local redeemBtn = nil
    local searchNames = {"submit","redeem","claim","confirm","enter","send","apply","ok","use","go","check"}
    local p = box.Parent
    for _ = 1, 8 do
        if not p then break end
        for _, d in ipairs(p:GetDescendants()) do
            if (d:IsA("TextButton") or d:IsA("ImageButton")) and not isOurGui(d) and d ~= box then
                local n = d.Name:lower()
                local txt = ""
                pcall(function() txt = d.Text:lower() end)
                for _, sn in ipairs(searchNames) do
                    if n:find(sn) or txt:find(sn) then
                        if isVisibleChain(d) then
                            redeemBtn = d
                            break
                        end
                    end
                end
                if redeemBtn then break end
            end
        end
        if redeemBtn then break end
        p = p.Parent
    end

    if redeemBtn then clickButton(redeemBtn) end
    fireBoxFocusLost(box)

    return true, "submitted via dynamic search"
end

local function aceCodeBox()
    local pg = playerGui
    local allBoxes = findAllTextBoxes(pg)
    for _, box in ipairs(allBoxes) do
        if isVisibleChain(box) then return box end
    end
    return nil
end

-- ==================== RIDDLE SOLVER LOGIC (OFFLINE) ====================
local BRAINROT_KNOWLEDGE = {
    -- Sammy's custom Steal a Brainrot riddle answers.
    { patterns = {"what is the first og"}, answer = "STRAWBERRYELEPHANT" },
    { patterns = {"what is the second og"}, answer = "MEOWL" },
    { patterns = {"what is the third og"}, answer = "SKIBIDITOILET" },
    { patterns = {"what is the fourth og"}, answer = "JOHNPORK" },
    { patterns = {"what is the rarest brainrot"}, answer = "HEADLESSHORSEMAN" },
    { patterns = {"what is the first mutation we added", "what was the first mutation we added", "what is the first mutation"}, answer = "BLOODROT" },
    { patterns = {"what is the latest mutation we added", "what was the latest mutation we added", "what is the latest mutation"}, answer = "CRYSTAL" },
    { patterns = {"what is the first brainrotgod we added", "what was the first brainrotgod we added", "what is the first brainrot god we added"}, answer = "ODINDINDUN" },
    { patterns = {"what is my name", "what is sammy name"}, answer = "SAMMY" },
    { patterns = {"what month is my birthday", "what month was i born", "what month am i born"}, answer = "FEBRUARY" },
    { patterns = {"what day is my birthday", "what day was i born", "what day am i born"}, answer = "24" },
    { patterns = {"what day and month is my birthday", "what day and month was i born", "what day and month am i born"}, answer = "FEBRUARY 24" },
    { patterns = {"what year am i born", "what year was i born", "what year is my birthday"}, answer = "2002" },
    { patterns = {"what day and month and year am i born", "what day month and year was i born", "what is my full birthday"}, answer = "February 24 2002" },
    { patterns = {"what is the first secret"}, answer = "LOSTRALALERITOS" },
    { patterns = {"what is steal a brainrot", "what is steal the brainrot", "what do you do in steal a brainrot", "main goal"}, answer = "steal brainrots" },
    { patterns = {"what do you steal", "what are you trying to steal", "stolen from other players"}, answer = "brainrots" },
    { patterns = {"what do brainrots generate", "what do brainrots make", "what do brainrots produce", "income"}, answer = "money" },
    { patterns = {"what currency", "what is the currency", "what do you use to buy", "buy brainrots"}, answer = "money" },
    { patterns = {"where do brainrots spawn", "where are brainrots found", "where can you find brainrots"}, answer = "conveyor" },
    { patterns = {"what carries the brainrots", "what moves the brainrots", "brainrot path"}, answer = "conveyor" },
    { patterns = {"where do you keep brainrots", "where are brainrots placed", "protect your brainrots"}, answer = "base" },
    { patterns = {"what protects your base", "how do you protect your base", "base protection"}, answer = "lock" },
    { patterns = {"what happens when the base is locked", "when your base is locked", "lock your base"}, answer = "safe" },
    { patterns = {"what is the rarest rarity", "highest rarity", "best rarity", "rarest brainrot"}, answer = "secret" },
    { patterns = {"what is the highest rarity", "rarity above godly", "rarity after godly"}, answer = "secret" },
    { patterns = {"what are the rarities", "brainrot rarities", "rarity order"}, answer = "common rare epic legendary mythical brainrot godly secret" },
    { patterns = {"what is rebirth", "what does rebirth do", "rebirth reset"}, answer = "reset for rewards" },
    { patterns = {"what do you get from rebirth", "rebirth reward", "why rebirth"}, answer = "rewards" },
    { patterns = {"what is a brainrot", "what are brainrots"}, answer = "collectible characters" },
    { patterns = {"how do you earn money", "how to make money", "make cash"}, answer = "brainrots" },
    { patterns = {"how do you get better brainrots", "get stronger brainrots", "better brainrot"}, answer = "steal them" },
    { patterns = {"how do you get brainrots", "where do brainrots come from", "where to find brainrots"}, answer = "conveyor" },
    { patterns = {"what happens if you leave your brainrot", "what happens when you leave base", "base unprotected"}, answer = "stolen" },
    { patterns = {"how do you make money fast", "fastest way to earn money", "best way to get money"}, answer = "steal" },
    { patterns = {"who is sammy", "who is the developer", "who is the creator"}, answer = "SAMMY" },
    { patterns = {"what is my favorite color", "what color do i like", "sammy favorite color"}, answer = "BLUE" },
    { patterns = {"what is my favorite sport", "what sport do i like", "sammy favorite sport"}, answer = "FOOTBALL" },
    { patterns = {"what is my favorite food", "what food do i like", "sammy favorite food"}, answer = "PIZZA" },
    { patterns = {"who is my favorite player", "my favorite football player", "who do i like in football"}, answer = "RONALDO" },
    { patterns = {"what is my favorite animal", "favorite pet", "what pet do i have"}, answer = "SPIDER" },
    { patterns = {"what is my favorite game", "what game do i play", "my favorite game is"}, answer = "ROBLOX" },
    { patterns = {"what is my favorite number", "lucky number", "what number do i like"}, answer = "7" },
    { patterns = {"what is my favorite number twice", "lucky number twice", "number twice"}, answer = "77" },
    { patterns = {"what is my name twice", "sammy name twice", "my name repeated twice"}, answer = "SAMMYSAMMY" },
    { patterns = {"what is my name three times", "sammy name three times", "my name repeated three times"}, answer = "SAMMYSAMMYSAMMY" },
    { patterns = {"what country am i from", "where is sammy from", "what is my nationality"}, answer = "BRAZIL" },
    { patterns = {"how old is sammy", "what is my age", "how old am i"}, answer = "24" },
    { patterns = {"what year was i born", "birth year", "when was i born"}, answer = "2002" },
    { patterns = {"what rarity is the best", "best rarity", "top rarity", "rarest rarity"}, answer = "secret" },
    { patterns = {"what rarity is the worst", "worst rarity", "common rarity"}, answer = "common" },
    { patterns = {"what is the main goal of the game", "what is the point of the game", "game objective"}, answer = "steal brainrots" },
    { patterns = {"how many players can fit in a server", "max players in server", "how many people in server"}, answer = "8" },
    { patterns = {"who did sammy fight", "who did i fight in war", "admin abuse war opponent"}, answer = "JANDEL" },
    { patterns = {"who won the admin war", "admin war winner", "who won the fight"}, answer = "GROWAGARDEN" },
    { patterns = {"what is the worst game on roblox", "most boring game", "boring roblox game"}, answer = "KEYBOARDESCAPE" },
    { patterns = {"what is the worst owner", "boring owner", "worst game owner"}, answer = "SECRETLOKII" },
    { patterns = {"what is the most boring game owner", "boring game owner"}, answer = "SECRETLOKII" },
    { patterns = {"who is the brother of hydra bunny", "hydra bunny brother", "brother of hydra"}, answer = "CERBERUS" },
    { patterns = {"what is the worst secret", "worst secret in sab", "sammy worst secret"}, answer = "KARKERKARKURKUR" },
    { patterns = {"what does fire represent", "fire stands for what", "fire meaning"}, answer = "DRAGON" },
    { patterns = {"who won the world cup", "world cup winner", "which country won world cup"}, answer = "SPAIN" },
    { patterns = {"what is the first secret in sab", "first secret", "first secret added"}, answer = "LOSTRALALERITOS" },
    { patterns = {"what is the first brainrotgod", "first god", "first brainrot god"}, answer = "ODINDINDUN" },
    { patterns = {"what is the newest machine", "latest machine", "most recent machine"}, answer = "LOSTRADERS" },
    { patterns = {"what is the newest mutation", "latest mutation", "most recent mutation"}, answer = "CRYSTAL" },
    { patterns = {"what is the first mutation", "oldest mutation", "first mutation added"}, answer = "BLOODROT" },
    { patterns = {"what is the fourth mutation", "4th mutation", "mutation 4"}, answer = "RAINBOW" },
    { patterns = {"what is the fifth mutation", "5th mutation", "mutation 5"}, answer = "CANDY" },
    { patterns = {"what is the sixth mutation", "6th mutation", "mutation 6"}, answer = "LAVA" },
    { patterns = {"what is the seventh mutation", "7th mutation", "mutation 7"}, answer = "GALAXY" },
    { patterns = {"what is the eighth mutation", "8th mutation", "mutation 8"}, answer = "YINYANG" },
    { patterns = {"what is the ninth mutation", "9th mutation", "mutation 9"}, answer = "RADIOACTIVE" },
    { patterns = {"what is the tenth mutation", "10th mutation", "mutation 10"}, answer = "CURSED" },
    { patterns = {"what is the eleventh mutation", "11th mutation", "mutation 11"}, answer = "DIVINE" },
    { patterns = {"what is the twelfth mutation", "12th mutation", "mutation 12"}, answer = "CYBER" },
    { patterns = {"what is the thirteenth mutation", "13th mutation", "mutation 13"}, answer = "PHANTOM" },
    { patterns = {"what is the evil mutation", "evil mutation name", "cursed mutation"}, answer = "CURSED" },
    { patterns = {"what is the angelic mutation", "angels mutation", "divine mutation"}, answer = "DIVINE" },
    { patterns = {"what mutation turns you green", "green mutation", "radioactive mutation"}, answer = "RADIOACTIVE" },
    { patterns = {"what mutation turns you purple", "purple mutation", "galaxy mutation"}, answer = "GALAXY" },
    { patterns = {"what mutation turns you yellow", "yellow mutation", "divine mutation color"}, answer = "DIVINE" },
    { patterns = {"what mutation turns you red", "red mutation", "cursed mutation color"}, answer = "CURSED" },
    { patterns = {"what mutation turns you orange", "orange mutation", "lava mutation color"}, answer = "LAVA" },
    -- SAB Database entries
    { patterns = {"real name", "sammy real name", "sammys real name", "my real name", "creator real name", "owner real name", "creator name"}, answer = "SAMMY" },
    { patterns = {"who created sab", "who made sab", "who made steal a brainrot", "who is the owner", "who owns sab", "owner", "creator", "dev", "developer", "made by", "created by"}, answer = "SAMMY" },
    { patterns = {"roblox username", "my roblox username", "sammy username", "sammy roblox name", "roblox name", "username", "my username"}, answer = "SPYDERSAMMY" },
    { patterns = {"where was i born", "where was i born at", "birthplace", "where i was born"}, answer = "ALGERIA" },
    { patterns = {"where am i from", "where is sammy from", "my country", "sammy country", "country", "where do i live", "where does sammy live", "sammy location"}, answer = "BRAZIL" },
    { patterns = {"nationality", "sammy nationality", "my nationality"}, answer = "BRAZILIAN" },
    { patterns = {"state", "my state", "sammy state", "city", "my city", "sammy city"}, answer = "SAOPAULO" },
    { patterns = {"favorite color", "fav color", "my color", "sammy color", "color", "favourite color"}, answer = "BLUE" },
    { patterns = {"favorite sport", "fav sport", "sport", "my sport"}, answer = "FOOTBALL" },
    { patterns = {"favorite football player", "fav football player", "my favorite football player", "football player", "favorite player", "fav player", "ronaldo"}, answer = "RONALDO" },
    { patterns = {"favorite food", "fav food", "my food", "food"}, answer = "PIZZA" },
    { patterns = {"favorite animal", "fav animal", "my animal", "my pet"}, answer = "SPIDER" },
    { patterns = {"favorite game", "fav game"}, answer = "ROBLOX" },
    { patterns = {"social media", "youtube channel", "my youtube", "sammy youtube", "discord", "discord server", "sammy discord", "twitter", "tiktok", "instagram"}, answer = "SPYDERSAMMY" },
    { patterns = {"game created on", "created on", "what day was the game created", "what day was sab created", "game creation day", "game made on", "sab made on", "day sab was made", "day sab was released", "what day was sab made", "what day was sab released", "sab release day"}, answer = "FRIDAY" },
    { patterns = {"release month", "month sab was made", "month the game was made", "month sab was released", "what month was sab made", "what month was sab released", "sab release month"}, answer = "MAY" },
    { patterns = {"release year", "year sab was created", "what year was sab created", "what year was the game created", "year the game was created", "year sab was made", "year the game was made", "year made", "year created", "what year was sab made", "year of sab"}, answer = "2025" },
    { patterns = {"when was sab made", "when made", "date made", "when was sab created", "release date", "when was sab released", "when was the game released", "game release date", "game release", "sab release", "game released", "sab released"}, answer = "MAY162025" },
    { patterns = {"first trait", "1st trait", "first trait created", "1st trait created", "first trait added"}, answer = "LIGHTNING" },
    { patterns = {"trait you get when struck by lightning", "struck by lightning", "lightning trait", "trait from lightning", "trait when struck by lightning", "lightning"}, answer = "MATEO" },
    { patterns = {"first mutation", "1st mutation", "mutation 1", "mutation number 1"}, answer = "GOLD" },
    { patterns = {"second mutation", "2nd mutation", "mutation 2", "mutation number 2"}, answer = "DIAMOND" },
    { patterns = {"third mutation", "3rd mutation", "mutation 3", "mutation number 3"}, answer = "BLOODROT" },
    { patterns = {"fourth mutation", "4th mutation", "mutation 4", "mutation number 4"}, answer = "RAINBOW" },
    { patterns = {"fifth mutation", "5th mutation", "mutation 5", "mutation number 5"}, answer = "CANDY" },
    { patterns = {"sixth mutation", "6th mutation", "mutation 6", "mutation number 6"}, answer = "LAVA" },
    { patterns = {"seventh mutation", "7th mutation", "mutation 7", "mutation number 7"}, answer = "GALAXY" },
    { patterns = {"eighth mutation", "8th mutation", "mutation 8", "mutation number 8"}, answer = "YINYANG" },
    { patterns = {"ninth mutation", "9th mutation", "mutation 9", "mutation number 9"}, answer = "RADIOACTIVE" },
    { patterns = {"tenth mutation", "10th mutation", "mutation 10", "mutation number 10"}, answer = "CURSED" },
    { patterns = {"eleventh mutation", "11th mutation", "mutation 11", "mutation number 11"}, answer = "DIVINE" },
    { patterns = {"twelfth mutation", "12th mutation", "mutation 12", "mutation number 12"}, answer = "CYBER" },
    { patterns = {"thirteenth mutation", "13th mutation", "mutation 13", "mutation number 13"}, answer = "PHANTOM" },
    { patterns = {"fourteenth mutation", "14th mutation", "mutation 14", "mutation number 14"}, answer = "CRYSTAL" },
    { patterns = {"evil mutation", "evil", "cursed mutation"}, answer = "CURSED" },
    { patterns = {"angelic mutation", "angelic", "divine mutation", "good mutation", "best mutation", "top mutation"}, answer = "DIVINE" },
    { patterns = {"latest mutation", "most recent mutation", "most recent", "newest mutation"}, answer = "CRYSTAL" },
    { patterns = {"divinecursed"}, answer = "DIVINECURSED" },
    { patterns = {"curseddivine"}, answer = "CURSEDDIVINE" },
    { patterns = {"green mutation", "turns green", "green"}, answer = "RADIOACTIVE" },
    { patterns = {"purple mutation", "turns purple", "purple"}, answer = "GALAXY" },
    { patterns = {"black and white mutation", "black mutation", "turns black", "black and white"}, answer = "YINYANG" },
    { patterns = {"yellow mutation", "turns yellow", "yellow"}, answer = "DIVINE" },
    { patterns = {"red mutation", "turns red", "red"}, answer = "CURSED" },
    { patterns = {"orange mutation", "turns orange", "orange"}, answer = "LAVA" },
    { patterns = {"first machine", "1st machine", "machine 1", "machine number 1"}, answer = "RAINBOWMACHINE" },
    { patterns = {"second machine", "2nd machine", "machine 2", "machine number 2"}, answer = "BUBBLEGUMMACHINE" },
    { patterns = {"third machine", "3rd machine", "machine 3", "machine number 3"}, answer = "FUSEMACHINE" },
    { patterns = {"fourth machine", "4th machine", "machine 4", "machine number 4"}, answer = "CRAFTMACHINE" },
    { patterns = {"fifth machine", "5th machine", "machine 5", "machine number 5"}, answer = "WITCHFUSE" },
    { patterns = {"sixth machine", "6th machine", "machine 6", "machine number 6"}, answer = "BRAINROTDEALER" },
    { patterns = {"seventh machine", "7th machine", "machine 7", "machine number 7"}, answer = "BRAINROTTRADER" },
    { patterns = {"eighth machine", "8th machine", "machine 8", "machine number 8"}, answer = "SANTASFUSE" },
    { patterns = {"ninth machine", "9th machine", "machine 9", "machine number 9"}, answer = "SANTASSHOP" },
    { patterns = {"tenth machine", "10th machine", "machine 10", "machine number 10"}, answer = "NEWYEARSMACHINE" },
    { patterns = {"eleventh machine", "11th machine", "machine 11", "machine number 11"}, answer = "DUELSMACHINE" },
    { patterns = {"twelfth machine", "12th machine", "machine 12", "machine number 12"}, answer = "CUPIDSMACHINE" },
    { patterns = {"thirteenth machine", "13th machine", "machine 13", "machine number 13"}, answer = "TRADEMACHINE" },
    { patterns = {"fourteenth machine", "14th machine", "machine 14", "machine number 14"}, answer = "DIVINEFUSE" },
    { patterns = {"fifteenth machine", "15th machine", "machine 15", "machine number 15"}, answer = "EGGINCUBATOR" },
    { patterns = {"sixteenth machine", "16th machine", "machine 16", "machine number 16"}, answer = "CYBERCRAFTMACHINE" },
    { patterns = {"seventeenth machine", "17th machine", "machine 17", "machine number 17"}, answer = "SUMMERFUSE" },
    { patterns = {"eighteenth machine", "18th machine", "machine 18", "machine number 18"}, answer = "LOSTRADERS" },
    { patterns = {"og brainrot cannot be obtained", "headless horseman", "rarest brainrot", "rarest", "unobtainable brainrot", "unobtainable"}, answer = "HEADLESSHORSEMAN" },
    { patterns = {"best brainrot"}, answer = "STRAWBERRYELEPHANT" },
    { patterns = {"first og added", "1st og", "og 1", "og number 1"}, answer = "STRAWBERRYELEPHANT" },
    { patterns = {"second og added", "2nd og", "og 2", "og number 2"}, answer = "MEOWL" },
    { patterns = {"third og added", "3rd og", "og 3", "og number 3"}, answer = "SKIBIDITOILET" },
    { patterns = {"fifth og added", "5th og", "og 5", "og number 5"}, answer = "JOHNPORK" },
    { patterns = {"highest rarity", "rarest rarity", "top rarity", "best rarity"}, answer = "OG" },
    { patterns = {"lowest rarity", "common rarity", "worst rarity"}, answer = "COMMON" },
    { patterns = {"fire represents", "fire stands for", "fire"}, answer = "DRAGON" },
    { patterns = {"won the world cup", "world cup winner", "world cup"}, answer = "SPAIN" },
    { patterns = {"worst game owner", "most boring game owner"}, answer = "SECRETLOKII" },
    { patterns = {"most boring game on roblox", "most boring game"}, answer = "KEYBOARDESCAPE" },
    { patterns = {"spawned during admin abuse war"}, answer = "RACOONINIJANDELINI" },
    { patterns = {"who did i fight in the admin abuse war", "fought in admin abuse war", "who did sammy fight"}, answer = "JANDEL" },
    { patterns = {"won the admin abuse war", "admin war winner", "who won admin war"}, answer = "GROWAGARDEN" },
    { patterns = {"worst secret", "bad secret"}, answer = "KARKERKARKURKUR" },
    { patterns = {"maximum server size", "max server size", "server size", "how many players", "max players", "player count", "players"}, answer = "EIGHT" },
    { patterns = {"brother of hydra bunny", "hydra bunny brother", "brother"}, answer = "CERBERUS" },
    { patterns = {"game name", "name of the game", "sab", "sab stands for", "full name", "steal a brainrot", "full game name", "game full name", "sab full name"}, answer = "STEALABRAINROT" },
    { patterns = {"number of mutations", "how many mutations", "mutation count", "brainrot count"}, answer = "14" },
    { patterns = {"total mutations"}, answer = "THIRTEEN" },
    { patterns = {"number of machines", "how many machines", "machine count"}, answer = "18" },
    { patterns = {"total machines"}, answer = "18" },
    { patterns = {"most popular brainrot"}, answer = "DRAGONCANNELONNI" },
    { patterns = {"most common brainrot", "common brainrot", "weakest brainrot"}, answer = "NOOBINIPIZZANINI" },
    { patterns = {"favorite number", "fav number", "lucky number", "number"}, answer = "SEVEN" },
    { patterns = {"my name twice", "my name 2 times", "name twice", "owner twice", "creator twice"}, answer = "SAMMYSAMMY" },
    { patterns = {"my name 3 times"}, answer = "SAMMYSAMMYSAMMY" },
    { patterns = {"my age twice", "my age 2 times"}, answer = "2424" },
    { patterns = {"my age 3 times"}, answer = "242424" },
    { patterns = {"favorite color twice", "favorite color 2 times", "my favorite color twice", "my favorite color 2 times"}, answer = "BLUEBLUE" },
    { patterns = {"favorite color 3 times", "favorite color three times", "my favorite color 3 times"}, answer = "BLUEBLUEBLUE" },
    { patterns = {"favorite color 5 times"}, answer = "BLUEBLUEBLUEBLUEBLUE" },
    { patterns = {"favorite sport twice"}, answer = "FOOTBALLFOOTBALL" },
    { patterns = {"favorite food twice"}, answer = "PIZZAPIZZA" },
    { patterns = {"favorite color, favorite sport"}, answer = "BLUEFOOTBALL" },
    { patterns = {"favorite color, favorite animal"}, answer = "BLUESPIDER" },
    { patterns = {"favorite color, favorite food"}, answer = "BLUEPIZZA" },
    { patterns = {"favorite color, favorite player"}, answer = "BLUERONALDO" },
    { patterns = {"favorite color, creator", "favorite color, owner"}, answer = "BLUESAMMY" },
    { patterns = {"favorite color, username"}, answer = "BLUESPYDERSAMMY" },
    { patterns = {"favorite color, my age"}, answer = "BLUE24" },
    { patterns = {"favorite color, my country"}, answer = "BLUEBRAZIL" },
    { patterns = {"favorite color, favorite sport, favorite animal"}, answer = "BLUEFOOTBALLSPIDER" },
    { patterns = {"game type", "what type", "sab genre", "sab type", "type of game", "game genre", "genre"}, answer = "SIMULATOR" },
    { patterns = {"first update", "1st update", "update"}, answer = "MUTATIONS" },
    { patterns = {"first brainrot added", "1st brainrot", "oldest brainrot"}, answer = "STRAWBERRYELEPHANT" },
    { patterns = {"newest machine", "last machine", "latest machine"}, answer = "LOSTRADERS" },
    { patterns = {"pet name", "pet"}, answer = "SPIDER" },
    { patterns = {"total rarities", "how many rarities", "number of rarities", "rarities", "rarity count"}, answer = "SIX" },
    { patterns = {"color of gold mutation", "gold"}, answer = "YELLOW" },
    { patterns = {"color of diamond mutation", "diamond"}, answer = "BLUE" },
    { patterns = {"color of bloodrot mutation"}, answer = "RED" },
    { patterns = {"color of rainbow mutation", "rainbow"}, answer = "RAINBOW" },
    { patterns = {"color of candy mutation", "candy"}, answer = "PINK" },
    { patterns = {"color of lava mutation", "lava"}, answer = "ORANGE" },
    { patterns = {"color of galaxy mutation", "galaxy"}, answer = "PURPLE" },
    { patterns = {"color of yinyang mutation", "yinyang"}, answer = "BLACK" },
    { patterns = {"color of radioactive mutation", "radioactive"}, answer = "GREEN" },
    { patterns = {"color of cursed mutation", "cursed"}, answer = "RED" },
    { patterns = {"color of divine mutation", "divine"}, answer = "YELLOW" },
    { patterns = {"color of cyber mutation", "cyber"}, answer = "BLUE" },
    { patterns = {"color of phantom mutation", "phantom"}, answer = "BLACK" },
    { patterns = {"color of crystal mutation", "crystal"}, answer = "BLUEPURPLE" },
    { patterns = {"blue mutation"}, answer = "DIAMOND" },
    { patterns = {"pink mutation"}, answer = "CANDY" },
    { patterns = {"what number is gold"}, answer = "1" },
    { patterns = {"what number is diamond"}, answer = "2" },
    { patterns = {"what number is bloodrot"}, answer = "3" },
    { patterns = {"what number is rainbow"}, answer = "4" },
    { patterns = {"what number is candy"}, answer = "5" },
    { patterns = {"what number is lava"}, answer = "6" },
    { patterns = {"what number is galaxy"}, answer = "7" },
    { patterns = {"what number is yinyang"}, answer = "8" },
    { patterns = {"what number is radioactive"}, answer = "9" },
    { patterns = {"what number is cursed"}, answer = "10" },
    { patterns = {"what number is divine"}, answer = "11" },
    { patterns = {"what number is cyber"}, answer = "12" },
    { patterns = {"what number is phantom"}, answer = "13" },
    { patterns = {"what number is crystal"}, answer = "14" },
    { patterns = {"rainbowmachine"}, answer = "RAINBOWMACHINE" },
    { patterns = {"bubblegummachine"}, answer = "BUBBLEGUMMACHINE" },
    { patterns = {"fusemachine"}, answer = "FUSEMACHINE" },
    { patterns = {"craftmachine"}, answer = "CRAFTMACHINE" },
    { patterns = {"witchfuse"}, answer = "WITCHFUSE" },
    { patterns = {"brainrotdealer"}, answer = "BRAINROTDEALER" },
    { patterns = {"brainrottrader"}, answer = "BRAINROTTRADER" },
    { patterns = {"santasfuse"}, answer = "SANTASFUSE" },
    { patterns = {"santasshop"}, answer = "SANTASSHOP" },
    { patterns = {"newyearsmachine"}, answer = "NEWYEARSMACHINE" },
    { patterns = {"duelsmachine"}, answer = "DUELSMACHINE" },
    { patterns = {"cupidsmachine"}, answer = "CUPIDSMACHINE" },
    { patterns = {"trademachine"}, answer = "TRADEMACHINE" },
    { patterns = {"divinefuse"}, answer = "DIVINEFUSE" },
    { patterns = {"eggincubator"}, answer = "EGGINCUBATOR" },
    { patterns = {"cybercraftmachine"}, answer = "CYBERCRAFTMACHINE" },
    { patterns = {"summerfuse"}, answer = "SUMMERFUSE" },
    { patterns = {"lostraders"}, answer = "LOSTRADERS" },
    { patterns = {"1st rarity", "first rarity"}, answer = "COMMON" },
    { patterns = {"2nd rarity", "second rarity"}, answer = "UNCOMMON" },
    { patterns = {"3rd rarity", "third rarity"}, answer = "RARE" },
    { patterns = {"4th rarity", "fourth rarity"}, answer = "EPIC" },
    { patterns = {"5th rarity", "fifth rarity"}, answer = "LEGENDARY" },
    { patterns = {"6th rarity", "sixth rarity"}, answer = "OG" },
    { patterns = {"uncommon"}, answer = "UNCOMMON" },
    { patterns = {"rare"}, answer = "RARE" },
    { patterns = {"epic"}, answer = "EPIC" },
    { patterns = {"legendary"}, answer = "LEGENDARY" },
    { patterns = {"og"}, answer = "OG" },
    { patterns = {"common"}, answer = "COMMON" },
    { patterns = {"sammy name", "game owner", "sammys name"}, answer = "SAMMY" },
    { patterns = {"birth country", "born in", "lives in", "comes from"}, answer = "BRAZIL" },
    { patterns = {"channel", "handle"}, answer = "SPYDERSAMMY" },
    { patterns = {"how many players in server", "server capacity", "players per server", "max server"}, answer = "EIGHT" },
    { patterns = {"day game released", "day released", "what day was it released", "what day was it created"}, answer = "FRIDAY" },
    { patterns = {"sab creation year", "creation year", "when created", "when released", "date released", "date created"}, answer = "MAY162025" },
    { patterns = {"trait added first"}, answer = "LIGHTNING" },
    { patterns = {"what trait", "lightning strike trait", "get struck by lightning", "lightning gives", "struck by lightning trait", "mateo"}, answer = "MATEO" },
    { patterns = {"fire symbol", "fire meaning", "fire brainrot"}, answer = "DRAGON" },
    { patterns = {"won world cup", "football world cup"}, answer = "SPAIN" },
    { patterns = {"boring owner", "most boring owner"}, answer = "SECRETLOKII" },
    { patterns = {"boring game", "boring roblox game"}, answer = "KEYBOARDESCAPE" },
    { patterns = {"admin abuse war", "admin war", "who won admin war"}, answer = "GROWAGARDEN" },
    { patterns = {"sammy fought", "fight in admin war"}, answer = "JANDEL" },
    { patterns = {"admin war brainrot", "spawned in admin war"}, answer = "RACOONINIJANDELINI" },
    { patterns = {"secret"}, answer = "KARKERKARKURKUR" },
    { patterns = {"cerberus brother", "brother of hydra"}, answer = "CERBERUS" },
    { patterns = {"how many brainrots", "number brainrots", "brainrot count"}, answer = "THIRTEEN" },
    { patterns = {"how many og brainrots", "total og brainrots", "og count"}, answer = "FIVE" },
}

local function normalizeCode(s)
    return (tostring(s or "")):match("^%s*(.-)%s*$") or ""
end

local function normalizeRiddleText(text)
    text = tostring(text or ""):lower()
    text = text:gsub("1st", "first"):gsub("2nd", "second")
    text = text:gsub("3rd", "third"):gsub("4th", "fourth")
    text = text:gsub("[^%w%s]", " ")
    text = text:gsub("%s+", " ")
    return text:match("^%s*(.-)%s*$") or ""
end

local function stemRiddleWord(word)
    word = tostring(word or ""):lower()
    if #word > 4 and word:sub(-3) == "ing" then word = word:sub(1, -4) end
    if #word > 4 and word:sub(-2) == "ed" then word = word:sub(1, -3) end
    if #word > 4 and word:sub(-2) == "es" then word = word:sub(1, -3) end
    if #word > 3 and word:sub(-1) == "s" then word = word:sub(1, -2) end
    local aliases = {
        original = "og",
        earliest = "first",
        newest = "latest",
        recent = "latest",
        made = "add",
        added = "add",
        created = "add",
        born = "birth",
        birthday = "birth",
        called = "name",
    }
    word = aliases[word] or word
    return word
end

local RIDDLE_FILLER_WORDS = {
    what = true, is = true, the = true, a = true, an = true,
    ["do"] = true, does = true, did = true, you = true, are = true,
    was = true, were = true, am = true, i = true, my = true,
    me = true, we = true, our = true, of = true, to = true,
    ["in"] = true, on = true, at = true, ["and"] = true, ["or"] = true,
    have = true, has = true, it = true, ["for"] = true, how = true,
    where = true, who = true, when = true, which = true, can = true,
    s = true,
    will = true, would = true, could = true, please = true,
}

local function riddleWords(text)
    local words = {}
    for word in normalizeRiddleText(text):gmatch("%w+") do
        local stem = stemRiddleWord(word)
        if stem ~= "" and not RIDDLE_FILLER_WORDS[stem] and not stem:match("^%d+$") then
            words[stem] = true
        end
    end
    if words.brainrot and words.god then words.brainrotgod = true end
    if words.brainrotgod then
        words.brainrot = true
        words.god = true
    end
    return words
end

local function riddlePatternMatches(messageText, pattern)
    local phrase = normalizeRiddleText(pattern)
    if messageText:find(phrase, 1, true) then return true end
    if messageText:gsub("%s+", ""):find(phrase:gsub("%s+", ""), 1, true) then return true end

    local wanted = riddleWords(pattern)
    local received = riddleWords(messageText)
    local wantedCount = 0
    for word in pairs(wanted) do
        wantedCount += 1
        if not received[word] then return false end
    end
    return wantedCount > 0
end

local function extractRiddleNumber(message, knownAnswer)
    local answerNumbers = {}
    for number in tostring(knownAnswer or ""):gmatch("%d+") do
        answerNumbers[number] = true
    end
    local lastNumber
    for number in tostring(message or ""):gmatch("%d+") do
        if not answerNumbers[number] then lastNumber = number end
    end
    return lastNumber
end

local function solveOfflineRiddle(message)
    if not message or message == "" then return nil end
    
    -- Check for combined questions (contains "and" or "&" or ",")
    local combinedAnswers = {}
    local parts = {}
    local temp = message
    temp = temp:gsub("%s*[&,]", " and ")
    local function splitByAnd(str)
        local result = {}
        local lower = str:lower()
        local pos = 1
        while true do
            local startPos, endPos = lower:find(" and ", pos, true)
            if not startPos then
                local remaining = str:sub(pos)
                if remaining ~= "" then
                    table.insert(result, remaining)
                end
                break
            end
            local part = str:sub(pos, startPos - 1)
            if part ~= "" then
                table.insert(result, part)
            end
            pos = endPos + 1
        end
        return result
    end
    parts = splitByAnd(temp)
    
    if #parts > 1 then
        local foundAny = false
        for _, part in ipairs(parts) do
            local trimmed = part:match("^%s*(.-)%s*$")
            if trimmed ~= "" and #trimmed > 2 then
                local normalizedPart = normalizeRiddleText(trimmed)
                local found = false
                for _, entry in ipairs(BRAINROT_KNOWLEDGE) do
                    for _, pattern in ipairs(entry.patterns) do
                        if riddlePatternMatches(normalizedPart, pattern) then
                            local answer = entry.answer:gsub("%s+", "")
                            table.insert(combinedAnswers, answer:upper())
                            found = true
                            foundAny = true
                            break
                        end
                    end
                    if found then break end
                end
                if not found then
                    if normalizedPart:find("who am i", 1, true) or normalizedPart:find("what am i", 1, true) then
                        table.insert(combinedAnswers, "PLAYER")
                        foundAny = true
                    elseif normalizedPart:find("yes or no", 1, true) and normalizedPart:find("brainrot", 1, true) then
                        table.insert(combinedAnswers, "YES")
                        foundAny = true
                    end
                end
            end
        end
        if foundAny and #combinedAnswers > 1 then
            local combined = table.concat(combinedAnswers)
            local suffix = extractRiddleNumber(message, "")
            if suffix and #suffix > 0 then
                combined = combined .. suffix
            end
            return combined, suffix
        end
    end
    
    -- Single answer lookup
    local normalized = normalizeRiddleText(message)
    if normalized == "" then return nil end
    
    for _, entry in ipairs(BRAINROT_KNOWLEDGE) do
        for _, pattern in ipairs(entry.patterns) do
            if riddlePatternMatches(normalized, pattern) then
                local answer = entry.answer:gsub("%s+", "")
                return answer:upper(), extractRiddleNumber(message, entry.answer)
            end
        end
    end

    if normalized:find("who am i", 1, true) or normalized:find("what am i", 1, true) then
        return "PLAYER", extractRiddleNumber(message, "player")
    end
    if normalized:find("yes or no", 1, true) and normalized:find("brainrot", 1, true) then
        return "YES", extractRiddleNumber(message, "yes")
    end
    return nil
end

local function solveRiddle(message, seq)
    if not _riddleSolver or not _enabled then return end
    _solving = _solving + 1
    if setStatus then setStatus("AI riddles: thinking...", Color3.fromRGB(200,200,200)) end
    task.spawn(function()
        task.wait()
        _solving = math.max(0, _solving - 1)
        if not _enabled or not _riddleSolver then return end

        local answer, suffix = solveOfflineRiddle(message)
        local top = normalizeCode(answer)
        top = top:gsub("%s+", "")
        if top and #top > 0 and suffix and #suffix > 0 then
            top = top .. suffix
        end
        if top and #top > 0 and seq >= _lastTypedSeq then
            _lastTypedSeq = seq
            _solvedCount = _solvedCount + 1
            if setStatus then setStatus("Answer: " .. top, Color3.fromRGB(0,255,0)) end
            if flashCode then flashCode(top) end
            appendToBox(top, true)  -- forceSubmit = true
        elseif not top or #top == 0 then
            if setStatus then setStatus("No answer found", Color3.fromRGB(255,0,0)) end
        end
    end)
end

-- ==================== CARD STYLE COLORS ====================
local COL_BG    = Color3.fromRGB(10, 10, 12)
local COL_BTN   = Color3.fromRGB(16, 17, 20)
local COL_WHITE = Color3.fromRGB(255, 255, 255)
local COL_TRACK = Color3.fromRGB(30, 31, 35)
local COL_ON    = Color3.fromRGB(0, 210, 100)

local COL_CHOCOLATE_DARK  = Color3.fromRGB(40, 20, 10)
local COL_CHOCOLATE_LIGHT = Color3.fromRGB(210, 180, 140)
local COL_CHOCOLATE_TITLE = Color3.fromRGB(230, 200, 160)
local COL_CHOCOLATE_VERY_LIGHT = Color3.fromRGB(230, 210, 190)

-- CLEANUP OLD GUIS --
pcall(function()
    for _, name in ipairs({"CardACECodeSniper", "ACECodeSniperUI", "AutoTypeCodesUI", "ACEPaste"}) do
        local previous = game.CoreGui:FindFirstChild(name)
        if previous then previous:Destroy() end
        local previous2 = playerGui:FindFirstChild(name)
        if previous2 then previous2:Destroy() end
    end
end)

-- ==================== CARD GUI ====================
local GUI = Instance.new("ScreenGui")
GUI.Name = "CardACECodeSniper"
GUI.ResetOnSpawn = false
GUI.IgnoreGuiInset = true
GUI.DisplayOrder = 999
if not pcall(function() GUI.Parent = game.CoreGui end) then GUI.Parent = playerGui end

-- Marco principal
local MainBorder = Instance.new("Frame")
MainBorder.Name = "MainBorder"
MainBorder.Size = UDim2.new(0, 230, 0, 390) -- Más alto para Riddle Solver
MainBorder.Position = UDim2.new(1, -250, 0.12, 0)
MainBorder.BackgroundTransparency = 1
MainBorder.BorderSizePixel = 0
MainBorder.Active = true
MainBorder.Parent = GUI

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14)
MainCorner.Parent = MainBorder

-- Fondo principal
local Panel = Instance.new("ImageLabel")
Panel.Name = "Panel"
Panel.Size = UDim2.new(1, 0, 1, 0)
Panel.Position = UDim2.new(0, 0, 0, 0)
Panel.BackgroundColor3 = COL_BG
Panel.BackgroundTransparency = 0
Panel.BorderSizePixel = 0
Panel.Image = "rbxassetid://121970011926221"
Panel.ImageTransparency = 0
Panel.ScaleType = Enum.ScaleType.Crop
Panel.Parent = MainBorder
Panel.ZIndex = 0
Instance.new("UICorner", Panel).CornerRadius = UDim.new(0, 14)

local TextureOverlay = Instance.new("ImageLabel")
TextureOverlay.Name = "TextureOverlay"
TextureOverlay.Size = UDim2.new(1, 0, 1, 0)
TextureOverlay.Position = UDim2.new(0, 0, 0, 0)
TextureOverlay.BackgroundTransparency = 1
TextureOverlay.BorderSizePixel = 0
TextureOverlay.Image = "rbxassetid://114335498177005"
TextureOverlay.ImageTransparency = 0.3
TextureOverlay.ScaleType = Enum.ScaleType.Crop
TextureOverlay.Parent = MainBorder
TextureOverlay.ZIndex = 1
Instance.new("UICorner", TextureOverlay).CornerRadius = UDim.new(0, 14)

-- Header: título y botón "-"
local HeaderFrame = Instance.new("Frame")
HeaderFrame.Size = UDim2.new(1, 0, 0, 34)
HeaderFrame.Position = UDim2.new(0, 0, 0, 6)
HeaderFrame.BackgroundTransparency = 1
HeaderFrame.Parent = MainBorder
HeaderFrame.ZIndex = 5

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Size = UDim2.new(1, -30, 0, 30)
TitleLabel.Position = UDim2.new(0, 0, 0, 2)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "🤎CHOCOLATE SNIPER🤎"
TitleLabel.TextColor3 = COL_CHOCOLATE_TITLE
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 16
TitleLabel.TextXAlignment = Enum.TextXAlignment.Center
TitleLabel.TextYAlignment = Enum.TextYAlignment.Center
TitleLabel.Parent = HeaderFrame
TitleLabel.ZIndex = 5

-- Botón de colapso "-"
local CollapseBtn = Instance.new("TextButton")
CollapseBtn.Name = "CollapseBtn"
CollapseBtn.Size = UDim2.new(0, 24, 0, 24)
CollapseBtn.Position = UDim2.new(1, -30, 0, 5)
CollapseBtn.BackgroundColor3 = COL_CHOCOLATE_DARK
CollapseBtn.BackgroundTransparency = 0.4
CollapseBtn.BorderSizePixel = 0
CollapseBtn.Text = "-"
CollapseBtn.TextColor3 = COL_CHOCOLATE_LIGHT
CollapseBtn.Font = Enum.Font.GothamBold
CollapseBtn.TextSize = 18
CollapseBtn.TextXAlignment = Enum.TextXAlignment.Center
CollapseBtn.TextYAlignment = Enum.TextYAlignment.Center
CollapseBtn.Parent = HeaderFrame
CollapseBtn.ZIndex = 6
Instance.new("UICorner", CollapseBtn).CornerRadius = UDim.new(1, 0)

-- Contenido del menú
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, 0, 1, -52)
Content.Position = UDim2.new(0, 0, 0, 52)
Content.BackgroundTransparency = 1
Content.Parent = MainBorder
Content.ZIndex = 3

-- ======= BOTÓN "CHOCO" INDEPENDIENTE =======
local ChocoFrame = Instance.new("Frame")
ChocoFrame.Name = "ChocoFrame"
ChocoFrame.Size = UDim2.new(0, 100, 0, 34)
ChocoFrame.Position = UDim2.new(1, -110, 0.12, 0)
ChocoFrame.BackgroundTransparency = 0
ChocoFrame.BorderSizePixel = 0
ChocoFrame.Parent = GUI
ChocoFrame.Visible = _collapsed
Instance.new("UICorner", ChocoFrame).CornerRadius = UDim.new(1, 0)

local ChocoBg = Instance.new("ImageLabel")
ChocoBg.Size = UDim2.new(1, 0, 1, 0)
ChocoBg.Position = UDim2.new(0, 0, 0, 0)
ChocoBg.BackgroundTransparency = 0
ChocoBg.BorderSizePixel = 0
ChocoBg.Image = "rbxassetid://121970011926221"
ChocoBg.ImageTransparency = 0
ChocoBg.ScaleType = Enum.ScaleType.Crop
ChocoBg.Parent = ChocoFrame
Instance.new("UICorner", ChocoBg).CornerRadius = UDim.new(1, 0)

local ChocoTexture = Instance.new("ImageLabel")
ChocoTexture.Size = UDim2.new(1, 0, 1, 0)
ChocoTexture.Position = UDim2.new(0, 0, 0, 0)
ChocoTexture.BackgroundTransparency = 1
ChocoTexture.BorderSizePixel = 0
ChocoTexture.Image = "rbxassetid://114335498177005"
ChocoTexture.ImageTransparency = 0.25
ChocoTexture.ScaleType = Enum.ScaleType.Crop
ChocoTexture.Parent = ChocoBg
Instance.new("UICorner", ChocoTexture).CornerRadius = UDim.new(1, 0)

local ChocoText = Instance.new("TextLabel")
ChocoText.Size = UDim2.new(1, 0, 1, 0)
ChocoText.BackgroundTransparency = 1
ChocoText.Text = "Choco"
ChocoText.TextColor3 = COL_CHOCOLATE_LIGHT
ChocoText.Font = Enum.Font.GothamBold
ChocoText.TextSize = 14
ChocoText.TextXAlignment = Enum.TextXAlignment.Center
ChocoText.TextYAlignment = Enum.TextYAlignment.Center
ChocoText.Parent = ChocoBg

local ChocoBtn = Instance.new("TextButton")
ChocoBtn.Size = UDim2.new(1, 0, 1, 0)
ChocoBtn.BackgroundTransparency = 1
ChocoBtn.Text = ""
ChocoBtn.Parent = ChocoFrame

-- Inicializar visibilidad
if _collapsed then
    MainBorder.Visible = false
    ChocoFrame.Visible = true
else
    MainBorder.Visible = true
    ChocoFrame.Visible = false
end

-- Log
local LogBorder = Instance.new("Frame")
LogBorder.Size = UDim2.new(1, -16, 0, 60)
LogBorder.Position = UDim2.new(0, 8, 0, 4)
LogBorder.BackgroundColor3 = Color3.fromRGB(255, 95, 95)
LogBorder.BorderSizePixel = 0
LogBorder.Parent = Content
Instance.new("UICorner", LogBorder).CornerRadius = UDim.new(0, 6)

local LogInner = Instance.new("Frame")
LogInner.Size = UDim2.new(1, -2, 1, -2)
LogInner.Position = UDim2.new(0, 1, 0, 1)
LogInner.BackgroundColor3 = Color3.fromRGB(55, 5, 5)
LogInner.BorderSizePixel = 0
LogInner.ClipsDescendants = true
LogInner.Parent = LogBorder
Instance.new("UICorner", LogInner).CornerRadius = UDim.new(0, 5)

local LogLabel = Instance.new("TextLabel")
LogLabel.Size = UDim2.new(1, -8, 1, -6)
LogLabel.Position = UDim2.new(0, 4, 0, 3)
LogLabel.BackgroundTransparency = 1
LogLabel.Text = _enabled and "> scanning for codes..." or "status: OFF"
LogLabel.TextColor3 = Color3.fromRGB(140, 140, 150)
LogLabel.Font = Enum.Font.Code
LogLabel.TextSize = 11
LogLabel.TextXAlignment = Enum.TextXAlignment.Left
LogLabel.TextYAlignment = Enum.TextYAlignment.Top
LogLabel.TextWrapped = true
LogLabel.Parent = LogInner

-- Status
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -16, 0, 16)
statusLabel.Position = UDim2.new(0, 8, 0, 68)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = _enabled and "Status: Scanning..." or "Status: OFF"
statusLabel.TextColor3 = _enabled and COL_ON or Color3.fromRGB(120, 120, 120)
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextSize = 11
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.Parent = Content

-- Función para actualizar estado visual de un toggle
local function updateToggleVisual(ref, state)
    if not ref or not ref.track or not ref.pin then return end
    if state then
        TweenService:Create(ref.track, TweenInfo.new(0.15), {BackgroundColor3 = COL_ON}):Play()
        TweenService:Create(ref.pin, TweenInfo.new(0.15), {Position = UDim2.new(1, -14, 0.5, -6)}):Play()
    else
        TweenService:Create(ref.track, TweenInfo.new(0.15), {BackgroundColor3 = COL_TRACK}):Play()
        TweenService:Create(ref.pin, TweenInfo.new(0.15), {Position = UDim2.new(0, 2, 0.5, -6)}):Play()
    end
end

-- Toggle helper
local function MakeToggle(labelText, y, defaultOn, onToggle, color, refName)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -16, 0, 26)
    row.Position = UDim2.new(0, 8, 0, y)
    row.BackgroundTransparency = 1
    row.Parent = Content

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -44, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelText
    lbl.TextColor3 = color or COL_CHOCOLATE_LIGHT
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row

    local ToggleBorder = Instance.new("Frame")
    ToggleBorder.Size = UDim2.new(0, 34, 0, 18)
    ToggleBorder.Position = UDim2.new(1, -34, 0.5, -9)
    ToggleBorder.BackgroundColor3 = COL_WHITE
    ToggleBorder.BorderSizePixel = 0
    ToggleBorder.Parent = row
    Instance.new("UICorner", ToggleBorder).CornerRadius = UDim.new(1, 0)

    local track = Instance.new("TextButton")
    track.Size = UDim2.new(1, -2, 1, -2)
    track.Position = UDim2.new(0, 1, 0, 1)
    track.BackgroundColor3 = defaultOn and COL_ON or COL_TRACK
    track.BorderSizePixel = 0
    track.Text = ""
    track.AutoButtonColor = false
    track.Parent = ToggleBorder
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

    local pin = Instance.new("Frame")
    pin.Size = UDim2.new(0, 12, 0, 12)
    pin.Position = defaultOn and UDim2.new(1, -14, 0.5, -6) or UDim2.new(0, 2, 0.5, -6)
    pin.BackgroundColor3 = COL_WHITE
    pin.BorderSizePixel = 0
    pin.Parent = track
    Instance.new("UICorner", pin).CornerRadius = UDim.new(1, 0)

    local state = defaultOn
    local ref = {track = track, pin = pin, state = state}
    if refName then toggleRefs[refName] = ref end

    track.MouseButton1Click:Connect(function()
        state = not state
        ref.state = state
        if state then
            TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = COL_ON}):Play()
            TweenService:Create(pin, TweenInfo.new(0.15), {Position = UDim2.new(1, -14, 0.5, -6)}):Play()
        else
            TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = COL_TRACK}):Play()
            TweenService:Create(pin, TweenInfo.new(0.15), {Position = UDim2.new(0, 2, 0.5, -6)}):Play()
        end
        if onToggle then onToggle(state) end
    end)
    return ref
end

-- Toggles (ordenados y con espacios)
MakeToggle("Code Sniper", 90, _enabled, function(state)
    _enabled = state
    savedConfig.codeSniper = state
    if not state and clearAceCapture then clearAceCapture() end
    statusLabel.Text = state and "Status: Scanning..." or "Status: OFF"
    statusLabel.TextColor3 = state and COL_ON or Color3.fromRGB(120, 120, 120)
    saveConfig()
end, nil, "codeSniper")

-- Riddle Solver (debajo de Code Sniper)
MakeToggle("Riddle Solver", 118, _riddleSolver, function(state)
    _riddleSolver = state
    savedConfig.riddleSolver = state
    saveConfig()
end, COL_CHOCOLATE_VERY_LIGHT, "riddleSolver")

MakeToggle("Auto submit", 146, _autoAccept, function(state)
    _autoAccept = state
    savedConfig.autoSubmit = state
    saveConfig()
end, nil, "autoSubmit")

-- Contador
local DelayRow = Instance.new("Frame")
DelayRow.Size = UDim2.new(1, -16, 0, 28)
DelayRow.Position = UDim2.new(0, 8, 0, 174)
DelayRow.BackgroundTransparency = 1
DelayRow.Parent = Content

local DelayLbl = Instance.new("TextLabel")
DelayLbl.Size = UDim2.new(0, 100, 1, 0)
DelayLbl.BackgroundTransparency = 1
DelayLbl.Text = "Submit after msgs"
DelayLbl.TextColor3 = COL_CHOCOLATE_LIGHT
DelayLbl.Font = Enum.Font.GothamMedium
DelayLbl.TextSize = 11
DelayLbl.TextXAlignment = Enum.TextXAlignment.Left
DelayLbl.Parent = DelayRow

local CounterBorder = Instance.new("Frame")
CounterBorder.Size = UDim2.new(0, 74, 0, 24)
CounterBorder.Position = UDim2.new(1, -74, 0.5, -12)
CounterBorder.BackgroundColor3 = COL_CHOCOLATE_LIGHT
CounterBorder.BorderSizePixel = 0
CounterBorder.Parent = DelayRow
Instance.new("UICorner", CounterBorder).CornerRadius = UDim.new(0, 6)

local CounterStroke = Instance.new("UIStroke")
CounterStroke.Color = COL_CHOCOLATE_LIGHT
CounterStroke.Thickness = 1.5
CounterStroke.Parent = CounterBorder

local CounterInner = Instance.new("Frame")
CounterInner.Size = UDim2.new(1, -2, 1, -2)
CounterInner.Position = UDim2.new(0, 1, 0, 1)
CounterInner.BackgroundColor3 = COL_CHOCOLATE_DARK
CounterInner.BorderSizePixel = 0
CounterInner.Parent = CounterBorder
Instance.new("UICorner", CounterInner).CornerRadius = UDim.new(0, 5)

local MinusBtn = Instance.new("TextButton")
MinusBtn.Size = UDim2.new(0, 20, 1, 0)
MinusBtn.BackgroundTransparency = 1
MinusBtn.Text = "-"
MinusBtn.TextColor3 = COL_WHITE
MinusBtn.Font = Enum.Font.GothamBold
MinusBtn.TextSize = 14
MinusBtn.Parent = CounterInner

local CountLbl = Instance.new("TextLabel")
CountLbl.Size = UDim2.new(0, 28, 1, 0)
CountLbl.Position = UDim2.new(0, 20, 0, 0)
CountLbl.BackgroundTransparency = 1
CountLbl.Text = tostring(_submitAfter)
CountLbl.TextColor3 = COL_WHITE
CountLbl.Font = Enum.Font.GothamBold
CountLbl.TextSize = 12
CountLbl.Parent = CounterInner

local PlusBtn = Instance.new("TextButton")
PlusBtn.Size = UDim2.new(0, 20, 1, 0)
PlusBtn.Position = UDim2.new(1, -20, 0, 0)
PlusBtn.BackgroundTransparency = 1
PlusBtn.Text = "+"
PlusBtn.TextColor3 = COL_WHITE
PlusBtn.Font = Enum.Font.GothamBold
PlusBtn.TextSize = 14
PlusBtn.Parent = CounterInner

local function updatePrioritizeState()
    if _submitAfter > 1 then
        _prioritize7 = false
        savedConfig.prioritize7 = false
        if toggleRefs.prioritize7 then
            toggleRefs.prioritize7.state = false
            updateToggleVisual(toggleRefs.prioritize7, false)
        end
    end
end

MinusBtn.MouseButton1Click:Connect(function()
    _submitAfter = math.max(1, _submitAfter - 1)
    CountLbl.Text = tostring(_submitAfter)
    savedConfig.submitAfter = _submitAfter
    updatePrioritizeState()
    if clearAceCapture then clearAceCapture() end
    saveConfig()
end)

PlusBtn.MouseButton1Click:Connect(function()
    _submitAfter = _submitAfter + 1
    CountLbl.Text = tostring(_submitAfter)
    savedConfig.submitAfter = _submitAfter
    updatePrioritizeState()
    if clearAceCapture then clearAceCapture() end
    saveConfig()
end)

-- Prioritize 7
MakeToggle("Prioritize 7 characters code", 206, _prioritize7, function(state)
    _prioritize7 = state
    savedConfig.prioritize7 = state
    if state then
        _submitAfter = 1
        CountLbl.Text = "1"
        savedConfig.submitAfter = 1
    end
    saveConfig()
end, COL_CHOCOLATE_VERY_LIGHT, "prioritize7")

-- Auto submit after retype
MakeToggle("auto submit 1 after retype invalid", 234, _autoSubmitAfterRetype, function(state)
    _autoSubmitAfterRetype = state
    savedConfig.autoSubmitAfterRetype = state
    saveConfig()
end, COL_CHOCOLATE_VERY_LIGHT, "autoSubmitAfterRetype")

-- Retype invalid
MakeToggle("Retype invalid", 262, _retypeInvalid, function(state)
    _retypeInvalid = state
    savedConfig.retypeInvalid = state
    saveConfig()
end, nil, "retypeInvalid")

-- Discord link
local DiscordLabel = Instance.new("TextLabel")
DiscordLabel.Size = UDim2.new(1, -16, 0, 18)
DiscordLabel.Position = UDim2.new(0, 8, 0, 290)
DiscordLabel.BackgroundTransparency = 1
DiscordLabel.Text = "https://discord.gg/6Et4mdsXnE"
DiscordLabel.TextColor3 = COL_CHOCOLATE_LIGHT
DiscordLabel.Font = Enum.Font.Gotham
DiscordLabel.TextSize = 10
DiscordLabel.TextXAlignment = Enum.TextXAlignment.Left
DiscordLabel.Parent = Content

-- ==================== COOLDOWN LOGIC ====================
local function startCooldown()
    if not _enabled then return end
    if _cooldownTask then
        task.cancel(_cooldownTask)
        _cooldownTask = nil
    end
    _cooldownActive = true
    clearAceCapture()

    setStatus("COOLDOWN: (5s)", COL_CHOCOLATE_LIGHT)

    _cooldownTask = task.spawn(function()
        for i = 4, 0, -1 do
            task.wait(1)
            if not _cooldownActive then break end
            if i == 0 then
                setStatus("Ready", COL_WHITE)
            else
                setStatus("COOLDOWN: ("..i.."s)", COL_CHOCOLATE_LIGHT)
            end
        end
        _cooldownActive = false
        _cooldownTask = nil
    end)
end

-- ==================== CODE SNIPER / REDEEM LOGIC ====================

function setStatus(msg, col)
    if not LogLabel or not _enabled then return end
    if msg == _lastStatusMsg then return end
    _lastStatusMsg = msg
    LogLabel.Text = tostring(msg)
    if col then LogLabel.TextColor3 = col end
end

function flashCode(code, col)
    if not code or code == "" or code == "—" then return end
    setStatus("[code] -> " .. tostring(code), col or COL_WHITE)
end

local function resetPasteCounter()
    _capturedParts = {}
    if not _retypeCarryText or os.clock() > _retypeCarryUntil then
        _retypeCarryText = nil
        _retypeCarryUntil = 0
    end
end
clearAceCapture = function()
    _capturedParts = {}
    _retypeCarryText = nil
    _retypeCarryUntil = 0
    _retypeCarryVersion += 1
    _pendingRetypeSubmit = false
end

local function clearBoxWatchers()
    if _boxTextConn then pcall(function() _boxTextConn:Disconnect() end) end
    if _boxAncestryConn then pcall(function() _boxAncestryConn:Disconnect() end) end
    for _, connection in ipairs(_boxVisibilityConns) do pcall(function() connection:Disconnect() end) end
    _boxTextConn = nil
    _boxAncestryConn = nil
    _boxVisibilityConns = {}
    _lastWatchedBox = nil
end

local function watchBoxForBlankReset(box)
    if not box or _lastWatchedBox == box then return end
    clearBoxWatchers()
    _lastWatchedBox = box
    if box.Text ~= "" then _lastNonBlankBoxText = box.Text end
    _boxTextConn = box:GetPropertyChangedSignal("Text"):Connect(function()
        if box.Text == "" then
            if UserInputService:GetFocusedTextBox() == box then
                startCooldown()
            end
        else
            _lastNonBlankBoxText = box.Text
            if _retypeCarryText and os.clock() <= _retypeCarryUntil then
                return
            end
        end
    end)
    _boxAncestryConn = box.AncestryChanged:Connect(function(_, parent)
        if not parent then clearAceCapture(); clearBoxWatchers(); _lastWatchedBox = nil end
    end)
end

UserInputService.TextBoxFocused:Connect(function(box)
    if box:IsDescendantOf(GUI) then return end
    if box ~= aceCodeBox() then return end
    _focused = box
    _lastBox = box
    watchBoxForBlankReset(box)
    if _enabled then
        startCooldown()
    end
end)

UserInputService.TextBoxFocusReleased:Connect(function(box)
    if box:IsDescendantOf(GUI) then return end
    local codeBox = aceCodeBox()
    if box ~= codeBox and box ~= _lastBox then return end
    if _retypeInvalid and rememberPendingSubmission and (box == codeBox or box == _lastBox) then
        local submittedText = box.Text ~= "" and box.Text or _lastNonBlankBoxText
        rememberPendingSubmission(box, submittedText, false)
    end
    if _focused == box then
        _focused = nil
    end
end)

clearPendingSubmission = function()
    _pendingRejectedToken += 1
    _pendingRejectedText = nil
    _pendingRejectedBox = nil
    _pendingRejectedUntil = 0
end

rememberPendingSubmission = function(box, text, replaceExisting)
    if not _retypeInvalid or not text or text == "" then return end
    if not replaceExisting and _pendingRejectedText and os.clock() <= _pendingRejectedUntil then return end
    _pendingRejectedToken += 1
    local token = _pendingRejectedToken
    _pendingRejectedText = text
    _pendingRejectedBox = box
    _pendingRejectedUntil = os.clock() + 8
    task.delay(8, function()
        if token == _pendingRejectedToken then clearPendingSubmission() end
    end)
end

local function restoreRejectedText(box, previousText)
    if not _retypeInvalid or not previousText or previousText == "" then return false end
    RunService.Heartbeat:Wait()
    local repasteBox = aceCodeBox() or box
    if not repasteBox or not isVisibleChain(repasteBox) then return false end
    _retypeCarryVersion += 1
    local carryVersion = _retypeCarryVersion
    _retypeCarryText = previousText
    _retypeCarryUntil = os.clock() + 8
    local restored = pcall(function() repasteBox.Text = previousText end)
    if restored then
        _lastBox = repasteBox
        watchBoxForBlankReset(repasteBox)
        task.defer(function()
            for _ = 1, 4 do
                if carryVersion ~= _retypeCarryVersion then return end
                if not _retypeCarryText or os.clock() > _retypeCarryUntil then return end
                local current = aceCodeBox() or repasteBox
                if current and isVisibleChain(current) then
                    pcall(function() current.Text = previousText end)
                    _lastBox = current
                end
                task.wait(0.06)
            end
        end)
        if _autoSubmitAfterRetype then
            _pendingRetypeSubmit = true
        end
    else
        _retypeCarryText = nil
        _retypeCarryUntil = 0
    end
    return restored
end

handleRedemptionFeedback = function(text, feedbackObject)
    if not _retypeInvalid or not _pendingRejectedText then return end
    if os.clock() > _pendingRejectedUntil then clearPendingSubmission(); return end
    if feedbackObject and feedbackObject:IsDescendantOf(GUI) then return end

    local lower = tostring(text or ""):lower()

    local rejected = lower:find("invalid code", 1, true)
        or lower:find("code is invalid", 1, true)
        or lower:find("expired", 1, true)
        or lower:find("already redeemed", 1, true)
        or lower:find("already used", 1, true)
        or lower:find("doesn't exist", 1, true)
        or lower:find("does not exist", 1, true)
        or lower:find("not found", 1, true)
        or lower:find("rejected", 1, true)
    if not rejected then return end
    local previousText = _pendingRejectedText
    local previousBox = _pendingRejectedBox
    local restored = restoreRejectedText(previousBox, previousText)
    clearPendingSubmission()
    if restored then
        setStatus("Invalid - repasted: " .. previousText, COL_CHOCOLATE_LIGHT)
        flashCode(previousText, Color3.fromRGB(255,90,90))
    end
end

function appendToBox(text, forceSubmit)
    if not text or text == "" then return end
    if _cooldownActive then return end
    if _lastWatchedBox and not isVisibleChain(_lastWatchedBox) then
        resetPasteCounter()
        clearBoxWatchers()
    end
    local box = aceCodeBox()
    local carryActive = _retypeCarryText and os.clock() <= _retypeCarryUntil
    if carryActive then
        _capturedParts = { _retypeCarryText, text }
        _retypeCarryText = nil
        _retypeCarryUntil = 0
        _retypeCarryVersion += 1
    else
        _capturedParts[#_capturedParts + 1] = text
    end
    local combinedCode = table.concat(_capturedParts)
    local capturedCount = #_capturedParts

    if box then
        _lastBox = box
        watchBoxForBlankReset(box)
        local boxWasFocused = UserInputService:GetFocusedTextBox() == box
        box.Text = combinedCode
        if boxWasFocused then
            pcall(function()
                local caretEnd = #combinedCode + 1
                box.CursorPosition = caretEnd
                box.SelectionStart = caretEnd
            end)
        end
    else
        setStatus("Captured; opening & searching UI...", COL_WHITE)
    end

    setStatus("Pasted " .. tostring(capturedCount) .. "/" .. tostring(_submitAfter), COL_WHITE)
    flashCode(combinedCode, COL_WHITE)

    local shouldSubmit = false
    if forceSubmit then
        shouldSubmit = true
    elseif _pendingRetypeSubmit then
        shouldSubmit = true
        _pendingRetypeSubmit = false
    elseif capturedCount >= _submitAfter then
        shouldSubmit = true
    end

    if shouldSubmit and _autoAccept then
        rememberPendingSubmission(box, combinedCode, true)
        local ok, statusMsg = typeAndSubmitCode(combinedCode)
        if not ok then
            local restored = restoreRejectedText(box, combinedCode)
            clearPendingSubmission()
            if restored and _autoSubmitAfterRetype then
                _pendingRetypeSubmit = true
            end
        end
        setStatus("Code: " .. combinedCode, COL_WHITE)
        flashCode(combinedCode, COL_WHITE)
        _capturedParts = {}
    end
end

-- ==================== EXTRACCIÓN DE CÓDIGO (Prioritize 7) ====================
local function extractCodeFromText(text)
    if not text or text == "" then return nil end
    local words = {}
    for word in text:gmatch("[%w_]+") do
        if word == string.upper(word) and #word >= 1 then
            words[#words+1] = word
        end
    end
    if #words == 0 then return nil end

    if _prioritize7 then
        for _, w in ipairs(words) do
            if #w >= 7 then
                return w
            end
        end
        return words[1]
    else
        return words[1]
    end
end

-- ==================== ANNOUNCEMENT NOTIFICATION LISTENER ====================
local function resolveNotifyRemote()
    if _G.PhiNotifyRemote then return _G.PhiNotifyRemote end
    local Net = ReplicatedStorage:FindFirstChild("Packages")
    if Net then Net = Net:FindFirstChild("Net") end
    if not Net then return nil end
    local getinfo = debug and (debug.getinfo or debug.info)
    if getgc and getinfo and getconnections then
        for _, d in ipairs(Net:GetDescendants()) do
            if d:IsA("RemoteEvent") then
                local ok, cs = pcall(getconnections, d.OnClientEvent)
                if ok then
                    for _, c in ipairs(cs) do
                        local f, fn = pcall(function() return c.Function end)
                        if f and type(fn) == "function" then
                            local i, info = pcall(getinfo, fn)
                            if i and tostring(info.short_src or info.source or ""):find("NotificationController", 1, true) then
                                return d
                            end
                        end
                    end
                end
            end
        end
    end
    return nil
end

local function aceStripRich(text)
    if type(text) ~= "string" then return tostring(text) end
    return (text:gsub("<[^>]->", ""))
end

local function aceTokenize(text)
    local words = {}
    for word in text:gmatch("[%w_]+") do
        words[#words + 1] = word
    end
    return words
end

local aceCollectBuffer = {}

local function onAceAnnouncement(...)
    if not _enabled then return end
    local text = aceStripRich(tostring((...) or ""))
    text = text:match("^%s*(.-)%s*$") or ""
    if text == "" then return end

    -- RIDDLE SOLVER: si está activo y el mensaje tiene espacios (es una pregunta)
    if _riddleSolver and text:find("%s") then
        _riddleSeq = _riddleSeq + 1
        solveRiddle(text, _riddleSeq)
        return
    end

    -- PRIORITIZE 7: si está activo, extraer código incluso con espacios
    if _prioritize7 then
        local code = extractCodeFromText(text)
        if code and not _seen[code] then
            _seen[code] = true
            task.delay(1.25, function() _seen[code] = nil end)
            appendToBox(code, true)
        end
        return
    end

    -- MODO NORMAL: solo palabras sin espacios
    if text:find("%s") then return end
    if _cooldownActive then return end

    for _, word in ipairs(aceTokenize(text)) do
        aceCollectBuffer[#aceCollectBuffer + 1] = word
    end
    
    local parts = {}
    for index = 1, math.min(#aceCollectBuffer, ACE_WORD_COUNT) do
        parts[index] = aceCollectBuffer[index]
    end
    if #aceCollectBuffer < ACE_WORD_COUNT then return end
    aceCollectBuffer = {}
    
    local captured = table.concat(parts)
    if captured == "" or _seen[captured] then return end
    _seen[captured] = true
    task.delay(1.25, function() _seen[captured] = nil end)
    appendToBox(captured, false)
end

local aceNotifyRemote = resolveNotifyRemote()
local aceListenConnection
if aceNotifyRemote then
    if getgenv then
        local previous = getgenv().ACECodeSniperNotifyConnection
        if previous then pcall(function() previous:Disconnect() end) end
    end
    aceListenConnection = aceNotifyRemote.OnClientEvent:Connect(function(...)
        if not _enabled then return end
        pcall(onAceAnnouncement, ...)
    end)
    if getgenv then getgenv().ACECodeSniperNotifyConnection = aceListenConnection end
end

-- ==================== WATCH REDEMPTION FEEDBACK ====================
local function watchRedemptionFeedbackObject(obj)
    if not (obj:IsA("TextLabel") or obj:IsA("TextButton")) then return end
    handleRedemptionFeedback(obj.Text or "", obj)
    obj:GetPropertyChangedSignal("Text"):Connect(function()
        handleRedemptionFeedback(obj.Text or "", obj)
    end)
end

for _, obj in ipairs(playerGui:GetDescendants()) do watchRedemptionFeedbackObject(obj) end
playerGui.DescendantAdded:Connect(function(obj)
    task.wait(0.04)
    watchRedemptionFeedbackObject(obj)
end)

-- ==================== COLLAPSE / EXPAND LOGIC ====================
CollapseBtn.MouseButton1Click:Connect(function()
    _collapsed = true
    savedConfig.collapsed = true
    MainBorder.Visible = false
    ChocoFrame.Visible = true
    saveConfig()
end)

ChocoBtn.MouseButton1Click:Connect(function()
    _collapsed = false
    savedConfig.collapsed = false
    MainBorder.Visible = true
    ChocoFrame.Visible = false
    saveConfig()
end)

-- ==================== STOP AURA ====================
if getgenv then
    getgenv().StopAura = function()
        if aceListenConnection then
            pcall(function() aceListenConnection:Disconnect() end)
            aceListenConnection = nil
        end
        if getgenv().ACECodeSniperNotifyConnection then
            pcall(function() getgenv().ACECodeSniperNotifyConnection:Disconnect() end)
            getgenv().ACECodeSniperNotifyConnection = nil
        end
        if GUI then GUI:Destroy() end
    end
end

-- ==================== DRAGGING ====================
local dragging, dragStart, startPos, dragInput = false, nil, nil, nil

local function startDrag(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainBorder.Visible and MainBorder.Position or ChocoFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end

local function updateDrag(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end

TitleLabel.InputBegan:Connect(startDrag)
TitleLabel.InputChanged:Connect(updateDrag)
ChocoBtn.InputBegan:Connect(startDrag)
ChocoBtn.InputChanged:Connect(updateDrag)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        if MainBorder.Visible then
            MainBorder.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        else
            ChocoFrame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end
end)

print("[Chocolate Sniper] Full Edition con Riddle Solver integrado")